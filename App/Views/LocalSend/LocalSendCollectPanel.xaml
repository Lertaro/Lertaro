<UserControl x:Class="Lertaro.App.Views.LocalSend.LocalSendCollectPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:services="clr-namespace:Lertaro.App.Services"
              AllowDrop="True" DragOver="Panel_DragOver" Drop="Panel_Drop">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Lertaro.App;component/Resources/Styles/Windows/LocalSendWindow.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="54"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="16,12,16,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Margin="0,0,0,10" Background="{DynamicResource ControlBackground}" CornerRadius="6" Padding="2" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content="文件 / 文件夹" IsChecked="{Binding IsFilesMode, Mode=OneWay}" Click="FilesMode_Click" Style="{StaticResource SegmentRadioButtonStyle}"/>
                    <RadioButton Content="{Binding Source={x:Static services:TranslationManager.Instance}, Path=[Settings_LocalSend_Text], Mode=OneWay}" IsChecked="{Binding IsTextMode, Mode=OneWay}" Click="TextMode_Click" Style="{StaticResource SegmentRadioButtonStyle}"/>
                </StackPanel>
            </Border>
            <Grid Grid.Row="1">
                <Grid.Style>
                    <Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsFilesMode}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style>
                </Grid.Style>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="添加文件" Click="AddFiles_Click" Style="{StaticResource SettingsButton}" Height="28" Padding="12,0" Margin="0,0,8,0"/>
                    <Button Content="添加文件夹" Click="AddFolder_Click" Style="{StaticResource SettingsButton}" Height="28" Padding="12,0"/>
                </StackPanel>
                <Border Grid.Row="1" Background="{DynamicResource FieldBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8">
                    <ListBox ItemsSource="{Binding CollectedItems}" Background="Transparent" BorderThickness="0" Margin="4" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="4" Padding="6,4" Margin="2,1">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverBackground}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}" ToolTip="{Binding Path}" FontSize="12" Foreground="{DynamicResource TextPrimary2}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" Content="&#xE74D;" Click="RemoveItem_Click" Style="{StaticResource RemoveItemButtonStyle}" ToolTip="移除" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="4,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.Style><Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsTextMode}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Grid.Style>
                <TextBox Text="{Binding TextToSend, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MultiLineTextBoxStyle}" Background="{DynamicResource FieldBackground}" Foreground="{DynamicResource TextPrimary}" CaretBrush="{DynamicResource TextPrimary}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="8,6" FontSize="12"/>
            </Grid>
        </Grid>
        <Border Grid.Row="1" Background="{DynamicResource FooterBg}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Padding="16,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="{Binding Source={x:Static services:TranslationManager.Instance}, Path=[Common_Cancel]}" Click="Cancel_Click" Style="{StaticResource SettingsButton}" Margin="0,0,8,0" Width="80" Height="30"/>
                <Button Content="下一步" Click="Next_Click" Style="{StaticResource PrimarySettingsButton}" IsEnabled="{Binding CanGoNextStep}" Width="85" Height="30"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
